#!/usr/bin#!/usr/bin/env python3
"""
检查环境变量并生成 .env 文件
""#!/usr/bin/env python3
"""
检查环境变量并生成 .env 文件
"""
import os

# 需要检查的#!/usr/bin/env python3
"""
检查环境变量并生成 .env 文件
"""
import os

# 需要检查的 AI 相关环境变量
AI_ENV_VARS = [
    'OPENAI_API_KEY',
    'ANTHROPIC_API_KEY',
#!/usr/bin/env python3
"""
检查环境变量并生成 .env 文件
"""
import os

# 需要检查的 AI 相关环境变量
AI_ENV_VARS = [
    'OPENAI_API_KEY',
    'ANTHROPIC_API_KEY',
    'GOOGLE_API_KEY',
    '#!/usr/bin/env python3
"""
检查环境变量并生成 .env 文件
"""
import os

# 需要检查的 AI 相关环境变量
AI_ENV_VARS = [
    'OPENAI_API_KEY',
    'ANTHROPIC_API_KEY',
    'GOOGLE_API_KEY',
    'AZURE_OPENAI_API_KEY',
    '#!/usr/bin/env python3
"""
检查环境变量并生成 .env 文件
"""
import os

# 需要检查的 AI 相关环境变量
AI_ENV_VARS = [
    'OPENAI_API_KEY',
    'ANTHROPIC_API_KEY',
    'GOOGLE_API_KEY',
    'AZURE_OPENAI_API_KEY',
    'AZURE_OPENAI_ENDPOINT',
    'AZ#!/usr/bin/env python3
"""
检查环境变量并生成 .env 文件
"""
import os

# 需要检查的 AI 相关环境变量
AI_ENV_VARS = [
    'OPENAI_API_KEY',
    'ANTHROPIC_API_KEY',
    'GOOGLE_API_KEY',
    'AZURE_OPENAI_API_KEY',
    'AZURE_OPENAI_ENDPOINT',
    'AZURE_OPENAI_MODEL',
    'SILICON#!/usr/bin/env python3
"""
检查环境变量并生成 .env 文件
"""
import os

# 需要检查的 AI 相关环境变量
AI_ENV_VARS = [
    'OPENAI_API_KEY',
    'ANTHROPIC_API_KEY',
    'GOOGLE_API_KEY',
    'AZURE_OPENAI_API_KEY',
    'AZURE_OPENAI_ENDPOINT',
    'AZURE_OPENAI_MODEL',
    'SILICONFLOW_API_KEY',
]

print("=" * 60)
print("检查 AI#!/usr/bin/env python3
"""
检查环境变量并生成 .env 文件
"""
import os

# 需要检查的 AI 相关环境变量
AI_ENV_VARS = [
    'OPENAI_API_KEY',
    'ANTHROPIC_API_KEY',
    'GOOGLE_API_KEY',
    'AZURE_OPENAI_API_KEY',
    'AZURE_OPENAI_ENDPOINT',
    'AZURE_OPENAI_MODEL',
    'SILICONFLOW_API_KEY',
]

print("=" * 60)
print("检查 AI 服务环境变量")
print("=" *#!/usr/bin/env python3
"""
检查环境变量并生成 .env 文件
"""
import os

# 需要检查的 AI 相关环境变量
AI_ENV_VARS = [
    'OPENAI_API_KEY',
    'ANTHROPIC_API_KEY',
    'GOOGLE_API_KEY',
    'AZURE_OPENAI_API_KEY',
    'AZURE_OPENAI_ENDPOINT',
    'AZURE_OPENAI_MODEL',
    'SILICONFLOW_API_KEY',
]

print("=" * 60)
print("检查 AI 服务环境变量")
print("=" * 60)

env_content = "##!/usr/bin/env python3
"""
检查环境变量并生成 .env 文件
"""
import os

# 需要检查的 AI 相关环境变量
AI_ENV_VARS = [
    'OPENAI_API_KEY',
    'ANTHROPIC_API_KEY',
    'GOOGLE_API_KEY',
    'AZURE_OPENAI_API_KEY',
    'AZURE_OPENAI_ENDPOINT',
    'AZURE_OPENAI_MODEL',
    'SILICONFLOW_API_KEY',
]

print("=" * 60)
print("检查 AI 服务环境变量")
print("=" * 60)

env_content = "# AI 服务提供商 API Key 配置\n#!/usr/bin/env python3
"""
检查环境变量并生成 .env 文件
"""
import os

# 需要检查的 AI 相关环境变量
AI_ENV_VARS = [
    'OPENAI_API_KEY',
    'ANTHROPIC_API_KEY',
    'GOOGLE_API_KEY',
    'AZURE_OPENAI_API_KEY',
    'AZURE_OPENAI_ENDPOINT',
    'AZURE_OPENAI_MODEL',
    'SILICONFLOW_API_KEY',
]

print("=" * 60)
print("检查 AI 服务环境变量")
print("=" * 60)

env_content = "# AI 服务提供商 API Key 配置\n"
env_content += "# 此文件#!/usr/bin/env python3
"""
检查环境变量并生成 .env 文件
"""
import os

# 需要检查的 AI 相关环境变量
AI_ENV_VARS = [
    'OPENAI_API_KEY',
    'ANTHROPIC_API_KEY',
    'GOOGLE_API_KEY',
    'AZURE_OPENAI_API_KEY',
    'AZURE_OPENAI_ENDPOINT',
    'AZURE_OPENAI_MODEL',
    'SILICONFLOW_API_KEY',
]

print("=" * 60)
print("检查 AI 服务环境变量")
print("=" * 60)

env_content = "# AI 服务提供商 API Key 配置\n"
env_content += "# 此文件已被添加到 .gitignore，不会被提交到 Git#!/usr/bin/env python3
"""
检查环境变量并生成 .env 文件
"""
import os

# 需要检查的 AI 相关环境变量
AI_ENV_VARS = [
    'OPENAI_API_KEY',
    'ANTHROPIC_API_KEY',
    'GOOGLE_API_KEY',
    'AZURE_OPENAI_API_KEY',
    'AZURE_OPENAI_ENDPOINT',
    'AZURE_OPENAI_MODEL',
    'SILICONFLOW_API_KEY',
]

print("=" * 60)
print("检查 AI 服务环境变量")
print("=" * 60)

env_content = "# AI 服务提供商 API Key 配置\n"
env_content += "# 此文件已被添加到 .gitignore，不会被提交到 Git\n\n"

found_any = False
for var in AI_ENV_VARS:
#!/usr/bin/env python3
"""
检查环境变量并生成 .env 文件
"""
import os

# 需要检查的 AI 相关环境变量
AI_ENV_VARS = [
    'OPENAI_API_KEY',
    'ANTHROPIC_API_KEY',
    'GOOGLE_API_KEY',
    'AZURE_OPENAI_API_KEY',
    'AZURE_OPENAI_ENDPOINT',
    'AZURE_OPENAI_MODEL',
    'SILICONFLOW_API_KEY',
]

print("=" * 60)
print("检查 AI 服务环境变量")
print("=" * 60)

env_content = "# AI 服务提供商 API Key 配置\n"
env_content += "# 此文件已被添加到 .gitignore，不会被提交到 Git\n\n"

found_any = False
for var in AI_ENV_VARS:
    value = os.getenv(var, '')
    if value:
        found_any = True#!/usr/bin/env python3
"""
检查环境变量并生成 .env 文件
"""
import os

# 需要检查的 AI 相关环境变量
AI_ENV_VARS = [
    'OPENAI_API_KEY',
    'ANTHROPIC_API_KEY',
    'GOOGLE_API_KEY',
    'AZURE_OPENAI_API_KEY',
    'AZURE_OPENAI_ENDPOINT',
    'AZURE_OPENAI_MODEL',
    'SILICONFLOW_API_KEY',
]

print("=" * 60)
print("检查 AI 服务环境变量")
print("=" * 60)

env_content = "# AI 服务提供商 API Key 配置\n"
env_content += "# 此文件已被添加到 .gitignore，不会被提交到 Git\n\n"

found_any = False
for var in AI_ENV_VARS:
    value = os.getenv(var, '')
    if value:
        found_any = True
        # 只显示前4位和后#!/usr/bin/env python3
"""
检查环境变量并生成 .env 文件
"""
import os

# 需要检查的 AI 相关环境变量
AI_ENV_VARS = [
    'OPENAI_API_KEY',
    'ANTHROPIC_API_KEY',
    'GOOGLE_API_KEY',
    'AZURE_OPENAI_API_KEY',
    'AZURE_OPENAI_ENDPOINT',
    'AZURE_OPENAI_MODEL',
    'SILICONFLOW_API_KEY',
]

print("=" * 60)
print("检查 AI 服务环境变量")
print("=" * 60)

env_content = "# AI 服务提供商 API Key 配置\n"
env_content += "# 此文件已被添加到 .gitignore，不会被提交到 Git\n\n"

found_any = False
for var in AI_ENV_VARS:
    value = os.getenv(var, '')
    if value:
        found_any = True
        # 只显示前4位和后4位
        masked = f"{value#!/usr/bin/env python3
"""
检查环境变量并生成 .env 文件
"""
import os

# 需要检查的 AI 相关环境变量
AI_ENV_VARS = [
    'OPENAI_API_KEY',
    'ANTHROPIC_API_KEY',
    'GOOGLE_API_KEY',
    'AZURE_OPENAI_API_KEY',
    'AZURE_OPENAI_ENDPOINT',
    'AZURE_OPENAI_MODEL',
    'SILICONFLOW_API_KEY',
]

print("=" * 60)
print("检查 AI 服务环境变量")
print("=" * 60)

env_content = "# AI 服务提供商 API Key 配置\n"
env_content += "# 此文件已被添加到 .gitignore，不会被提交到 Git\n\n"

found_any = False
for var in AI_ENV_VARS:
    value = os.getenv(var, '')
    if value:
        found_any = True
        # 只显示前4位和后4位
        masked = f"{value[:4]}...{value[-4:]}#!/usr/bin/env python3
"""
检查环境变量并生成 .env 文件
"""
import os

# 需要检查的 AI 相关环境变量
AI_ENV_VARS = [
    'OPENAI_API_KEY',
    'ANTHROPIC_API_KEY',
    'GOOGLE_API_KEY',
    'AZURE_OPENAI_API_KEY',
    'AZURE_OPENAI_ENDPOINT',
    'AZURE_OPENAI_MODEL',
    'SILICONFLOW_API_KEY',
]

print("=" * 60)
print("检查 AI 服务环境变量")
print("=" * 60)

env_content = "# AI 服务提供商 API Key 配置\n"
env_content += "# 此文件已被添加到 .gitignore，不会被提交到 Git\n\n"

found_any = False
for var in AI_ENV_VARS:
    value = os.getenv(var, '')
    if value:
        found_any = True
        # 只显示前4位和后4位
        masked = f"{value[:4]}...{value[-4:]}" if len(value) > 8 else "***"
        print(f"✓ {var#!/usr/bin/env python3
"""
检查环境变量并生成 .env 文件
"""
import os

# 需要检查的 AI 相关环境变量
AI_ENV_VARS = [
    'OPENAI_API_KEY',
    'ANTHROPIC_API_KEY',
    'GOOGLE_API_KEY',
    'AZURE_OPENAI_API_KEY',
    'AZURE_OPENAI_ENDPOINT',
    'AZURE_OPENAI_MODEL',
    'SILICONFLOW_API_KEY',
]

print("=" * 60)
print("检查 AI 服务环境变量")
print("=" * 60)

env_content = "# AI 服务提供商 API Key 配置\n"
env_content += "# 此文件已被添加到 .gitignore，不会被提交到 Git\n\n"

found_any = False
for var in AI_ENV_VARS:
    value = os.getenv(var, '')
    if value:
        found_any = True
        # 只显示前4位和后4位
        masked = f"{value[:4]}...{value[-4:]}" if len(value) > 8 else "***"
        print(f"✓ {var}: {masked}")
        env_content +=#!/usr/bin/env python3
"""
检查环境变量并生成 .env 文件
"""
import os

# 需要检查的 AI 相关环境变量
AI_ENV_VARS = [
    'OPENAI_API_KEY',
    'ANTHROPIC_API_KEY',
    'GOOGLE_API_KEY',
    'AZURE_OPENAI_API_KEY',
    'AZURE_OPENAI_ENDPOINT',
    'AZURE_OPENAI_MODEL',
    'SILICONFLOW_API_KEY',
]

print("=" * 60)
print("检查 AI 服务环境变量")
print("=" * 60)

env_content = "# AI 服务提供商 API Key 配置\n"
env_content += "# 此文件已被添加到 .gitignore，不会被提交到 Git\n\n"

found_any = False
for var in AI_ENV_VARS:
    value = os.getenv(var, '')
    if value:
        found_any = True
        # 只显示前4位和后4位
        masked = f"{value[:4]}...{value[-4:]}" if len(value) > 8 else "***"
        print(f"✓ {var}: {masked}")
        env_content += f"{var}={value}\n"
    else:
        print(f"✗#!/usr/bin/env python3
"""
检查环境变量并生成 .env 文件
"""
import os

# 需要检查的 AI 相关环境变量
AI_ENV_VARS = [
    'OPENAI_API_KEY',
    'ANTHROPIC_API_KEY',
    'GOOGLE_API_KEY',
    'AZURE_OPENAI_API_KEY',
    'AZURE_OPENAI_ENDPOINT',
    'AZURE_OPENAI_MODEL',
    'SILICONFLOW_API_KEY',
]

print("=" * 60)
print("检查 AI 服务环境变量")
print("=" * 60)

env_content = "# AI 服务提供商 API Key 配置\n"
env_content += "# 此文件已被添加到 .gitignore，不会被提交到 Git\n\n"

found_any = False
for var in AI_ENV_VARS:
    value = os.getenv(var, '')
    if value:
        found_any = True
        # 只显示前4位和后4位
        masked = f"{value[:4]}...{value[-4:]}" if len(value) > 8 else "***"
        print(f"✓ {var}: {masked}")
        env_content += f"{var}={value}\n"
    else:
        print(f"✗ {var}: 未设置")
        env#!/usr/bin/env python3
"""
检查环境变量并生成 .env 文件
"""
import os

# 需要检查的 AI 相关环境变量
AI_ENV_VARS = [
    'OPENAI_API_KEY',
    'ANTHROPIC_API_KEY',
    'GOOGLE_API_KEY',
    'AZURE_OPENAI_API_KEY',
    'AZURE_OPENAI_ENDPOINT',
    'AZURE_OPENAI_MODEL',
    'SILICONFLOW_API_KEY',
]

print("=" * 60)
print("检查 AI 服务环境变量")
print("=" * 60)

env_content = "# AI 服务提供商 API Key 配置\n"
env_content += "# 此文件已被添加到 .gitignore，不会被提交到 Git\n\n"

found_any = False
for var in AI_ENV_VARS:
    value = os.getenv(var, '')
    if value:
        found_any = True
        # 只显示前4位和后4位
        masked = f"{value[:4]}...{value[-4:]}" if len(value) > 8 else "***"
        print(f"✓ {var}: {masked}")
        env_content += f"{var}={value}\n"
    else:
        print(f"✗ {var}: 未设置")
        env_content += f"{var}=\n"

print("=" * 60)
#!/usr/bin/env python3
"""
检查环境变量并生成 .env 文件
"""
import os

# 需要检查的 AI 相关环境变量
AI_ENV_VARS = [
    'OPENAI_API_KEY',
    'ANTHROPIC_API_KEY',
    'GOOGLE_API_KEY',
    'AZURE_OPENAI_API_KEY',
    'AZURE_OPENAI_ENDPOINT',
    'AZURE_OPENAI_MODEL',
    'SILICONFLOW_API_KEY',
]

print("=" * 60)
print("检查 AI 服务环境变量")
print("=" * 60)

env_content = "# AI 服务提供商 API Key 配置\n"
env_content += "# 此文件已被添加到 .gitignore，不会被提交到 Git\n\n"

found_any = False
for var in AI_ENV_VARS:
    value = os.getenv(var, '')
    if value:
        found_any = True
        # 只显示前4位和后4位
        masked = f"{value[:4]}...{value[-4:]}" if len(value) > 8 else "***"
        print(f"✓ {var}: {masked}")
        env_content += f"{var}={value}\n"
    else:
        print(f"✗ {var}: 未设置")
        env_content += f"{var}=\n"

print("=" * 60)

if found_any:
    # 写入