# 重构计划：删除冗余数据库表

## 重构目标

分析并删除与主表字段冗余的独立表，同时不影响任何现有功能。

## 当前状态分析

### 冗余表汇总

| 独立表 | 主表已有字段 | 冗余程度 | 当前使用情况 |
|--------|-------------|---------|-------------|
| `CharacterTrait` | `core_traits` | 完全冗余 | 仅有简单CRUD API，前端未实际调用 |
| `CharacterAbility` | `special_abilities`, `physical_abilities` | 完全冗余 | 仅有简单CRUD API，前端未实际调用 |
| `CharacterRelationship` | `family_members`, `close_friends` | 完全冗余 | 仅有简单CRUD API，前端未实际调用 |
| `CharacterBackground` | `background`, `growth_experience` | 部分冗余 | **正在使用** - 提供结构化周期背景数据 |
| `FactionStructure` | `hierarchy`, `department_setup` | 完全冗余 | 仅有简单CRUD API，前端未实际调用 |
| `FactionGoal` | `short_term_goals`, `long_term_vision` | 完全冗余 | 仅有简单CRUD API，前端未实际调用 |
| `LocationStructure` | `overall_layout`, `functional_areas` | 完全冗余 | 仅有简单CRUD API，前端未实际调用 |
| `PowerLevel` | `typical_power_level` (SocialClass) | **不冗余** | 世界观能量体系修炼等级，有完整结构 |

### 详细分析

#### 1. CharacterTrait (完全冗余)
- **独立表结构**: `id`, `project_id`, `name`, `description`
- **主表字段**: `Character.core_traits` (Text类型，存储文本)
- **结论**: 主表已存储核心特质文本，独立表只是简单名称列表

#### 2. CharacterAbility (完全冗余)
- **独立表结构**: `id`, `project_id`, `name`, `description`
- **主表字段**: `Character.special_abilities`, `Character.physical_abilities`
- **注意**: `CharacterAbilityDetail` 是有用的（提供结构化能力详情），不应删除

#### 3. CharacterRelationship (完全冗余)
- **独立表结构**: `id`, `project_id`, `name`, `description`
- **主表字段**: `Character.family_members`, `Character.close_friends`
- **注意**: `Relationship` 表（使用 source_type/target_id）是通用的关系网络，与此不同

#### 4. CharacterBackground (部分冗余 - 保留)
- **独立表结构**: `id`, `character_id`, `period_name`, `start_age`, `end_age`, `key_events`, `traumas`...
- **主表字段**: `Character.background`, `Character.growth_experience`
- **结论**: 提供结构化周期背景（童年/少年/成年），比主表文本更结构化
- **操作**: **保留此表**

#### 5. FactionStructure (完全冗余)
- **独立表结构**: `id`, `project_id`, `name`, `description`
- **主表字段**: `Faction.hierarchy`, `Faction.department_setup`

#### 6. FactionGoal (完全冗余)
- **独立表结构**: `id`, `project_id`, `name`, `description`
- **主表字段**: `Faction.short_term_goals`, `Faction.long_term_vision`

#### 7. LocationStructure (完全冗余)
- **独立表结构**: `id`, `project_id`, `name`, `description`
- **主表字段**: `Location.overall_layout`, `Location.functional_areas`

#### 8. PowerLevel (不冗余)
- **独立表结构**: `id`, `world_id`, `name`, `level`, `level_name`, `description`, `requirements`, `characteristics`, `abilities`...
- **SocialClass字段**: `typical_power_level` (Integer)
- **结论**: PowerLevel是完整的修炼等级体系表，SocialClass只是引用其ID，不冗余

---

## 目标状态

删除以下6个完全冗余的独立表及其关联API：

1. `CharacterTrait` (character_trait表)
2. `CharacterAbility` (character_ability表)
3. `CharacterRelationship` (character_relationship表)
4. `FactionStructure` (faction_structure表)
5. `FactionGoal` (faction_goal表)
6. `LocationStructure` (location_structure表)

保留：
- `CharacterBackground` (有结构化数据)
- `CharacterAbilityDetail` (有结构化数据)
- `PowerLevel` (独立体系)

---

## 受影响文件

| 文件 | 变更类型 | 依赖关系 |
|------|---------|---------|
| `backend/app/models.py` | 修改 | 删除6个Model类 |
| `backend/app/api/setting_character.py` | 修改 | 删除3个API路由 |
| `backend/app/api/setting_faction.py` | 修改 | 删除2个API路由 |
| `backend/app/api/setting_location.py` | 修改 | 删除1个API路由 |
| `frontend/src/services/api.js` | 修改 | 删除6组API调用 |

---

## 执行计划

### 阶段0：测试脚本编写与验证

**目标**：编写测试脚本验证各表是否被实际使用

#### 0.1 编写数据库表使用情况检测脚本

创建 `backend/scripts/check_table_usage.py`:

```python
"""
检测冗余表是否被实际使用的脚本
"""
import sys
sys.path.insert(0, '.')

from app import create_app, db
from app.models import (
    CharacterTrait, CharacterAbility, CharacterRelationship,
    FactionStructure, FactionGoal, LocationStructure,
    CharacterBackground, CharacterAbilityDetail
)

app = create_app()

def check_table_usage():
    with app.app_context():
        print("=" * 60)
        print("数据库表使用情况检测")
        print("=" * 60)

        # 检测各表数据量
        tables = [
            ("character_trait", CharacterTrait),
            ("character_ability", CharacterAbility),
            ("character_relationship", CharacterRelationship),
            ("character_backgrounds", CharacterBackground),
            ("character_ability_details", CharacterAbilityDetail),
            ("faction_structure", FactionStructure),
            ("faction_goal", FactionGoal),
            ("location_structure", LocationStructure),
        ]

        for table_name, model in tables:
            count = model.query.count()
            print(f"{table_name}: {count} 条记录")

        print("=" * 60)
        print("检测完成")
        print("=" * 60)

if __name__ == '__main__':
    check_table_usage()
```

#### 0.2 编写前端API调用检测脚本

创建 `backend/scripts/check_api_usage.py`:

```python
"""
检测前端API调用情况的脚本
通过分析前端代码确认API是否被使用
"""
import os
import re

def check_api_usage():
    print("=" * 60)
    print("前端API使用情况检测")
    print("=" * 60)

    # 需要检测的API路径
    apis_to_check = [
        '/settings/character-trait',
        '/settings/character-ability',
        '/settings/character-relationship',
        '/settings/faction-structure',
        '/settings/faction-goal',
        '/settings/location-structure',
    ]

    # 前端API文件路径
    api_file = 'frontend/src/services/api.js'

    if not os.path.exists(api_file):
        print(f"文件不存在: {api_file}")
        return

    with open(api_file, 'r', encoding='utf-8') as f:
        content = f.read()

    for api in apis_to_check:
        # 检查API是否在api.js中定义
        if api.replace('/', '\\/') in content or api in content:
            print(f"[存在API定义] {api}")
        else:
            print(f"[未找到] {api}")

    print("=" * 60)
    print("检测完成")
    print("=" * 60)

if __name__ == '__main__':
    check_api_usage()
```

#### 0.3 执行测试脚本验证

```bash
# 数据库表使用情况
cd backend
python .venv\Scripts\python.exe scripts\check_table_usage.py

# 前端API调用情况
python scripts\check_api_usage.py
```

**验证标准**：
- 数据量为0的表可直接删除
- API未被前端调用的可直接删除
- 两者都未使用的表可以安全删除

---

### 阶段1：前端清理
- [ ] 1.1 从 `frontend/src/services/api.js` 删除6组冗余API调用
  - `getCharacterTraits` / `createCharacterTrait` / `updateCharacterTrait` / `deleteCharacterTrait`
  - `getCharacterAbilities` / `createCharacterAbility` / `updateCharacterAbility` / `deleteCharacterAbility`
  - `getCharacterRelationships` / `createCharacterRelationship` / `updateCharacterRelationship` / `deleteCharacterRelationship`
  - `getFactionStructures` / `createFactionStructure` / `updateFactionStructure` / `deleteFactionStructure`
  - `getFactionGoals` / `createFactionGoal` / `updateFactionGoal` / `deleteFactionGoal`
  - `getLocationStructures` / `createLocationStructure` / `updateLocationStructure` / `deleteLocationStructure`
- [ ] 1.2 验证前端构建无错误

### 阶段2：后端API清理
- [ ] 2.1 从 `backend/app/api/setting_character.py` 删除
  - CharacterTrait 相关路由 (lines 6-64)
  - CharacterAbility 相关路由 (lines 66-124)
  - CharacterRelationship 相关路由 (lines 126-184)
- [ ] 2.2 从 `backend/app/api/setting_faction.py` 删除
  - FactionStructure 相关路由 (lines 6-64)
  - FactionGoal 相关路由 (lines 66-124)
- [ ] 2.3 从 `backend/app/api/setting_location.py` 删除
  - LocationStructure 相关路由 (lines 6-64)
- [ ] 2.4 验证后端启动无错误

### 阶段3：模型清理
- [ ] 3.1 从 `backend/app/models.py` 删除6个Model类
  - `CharacterTrait` (lines 1293-1310)
  - `CharacterAbility` (lines 1814-1831)
  - `CharacterRelationship` (lines 1833-1850)
  - `FactionStructure` (lines 1852-1869)
  - `FactionGoal` (lines 1871-1888)
  - `LocationStructure` (lines 1890-1907)
- [ ] 3.2 验证模型导入无错误

### 阶段4：数据库迁移
- [ ] 4.1 创建数据库迁移脚本删除6个表
  - `character_trait`
  - `character_ability`
  - `character_relationship`
  - `faction_structure`
  - `faction_goal`
  - `location_structure`
- [ ] 4.2 测试迁移脚本执行

### 阶段5：验证
- [ ] 5.1 启动后端服务，验证无导入错误
- [ ] 5.2 启动前端服务，验证无构建错误
- [ ] 5.3 测试核心功能（角色、势力、地点创建/编辑/删除）

---

## 回滚计划

如果出现问题：
1. 恢复 `backend/app/models.py` 中删除的6个Model类
2. 恢复 `backend/app/api/setting_character.py` 中删除的路由
3. 恢复 `backend/app/api/setting_faction.py` 中删除的路由
4. 恢复 `backend/app/api/setting_location.py` 中删除的路由
5. 恢复 `frontend/src/services/api.js` 中删除的API调用
6. 回滚数据库迁移

---

## 风险评估

- **风险**: 可能有未知的前端组件使用了这些API
- **缓解**: 先搜索确认无使用，再删除
- **风险**: 数据库中可能存储了有用数据
- **缓解**: 迁移前备份数据库

---

## 确认事项

执行前请确认：
- [ ] 已备份数据库
- [ ] 已验证无前端组件调用这些API
- [ ] 已确认 `PowerLevel` 表不在删除范围内
- [ ] 已确认 `CharacterBackground` 表保留
