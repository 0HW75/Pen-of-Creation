## 修复方案

### 步骤1：统一数据库配置
修改 `app/__init__.py`，使用绝对路径配置数据库：
```python
import os
basedir = os.path.abspath(os.path.dirname(__file__))
app.config['SQLALCHEMY_DATABASE_URI'] = f'sqlite:///{os.path.join(basedir, "novel_editor.db")}'
```

### 步骤2：备份现有数据
备份 `app/novel_editor.db`（这是实际使用的数据库）

### 步骤3：清理和统一
1. 删除根目录的 `novel_editor.db`（空数据库，无用）
2. 确保 `app/novel_editor.db` 是唯一的生产数据库
3. 更新 Alembic 配置指向正确路径

### 步骤4：验证修复
1. 重启后端服务
2. 测试创建角色功能
3. 确认数据库路径一致

### 预期结果
- 只有一个数据库文件 `app/novel_editor.db`
- 所有脚本和应用使用同一个数据库
- 数据一致性得到保证